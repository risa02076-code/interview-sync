# 진행 상황

인터뷰싱크 — 후보자 희망시간·면접관 캘린더·회의실을 자동으로 대조해 면접 일정을 확정하고, 변경이 생기면 자동으로 재조율하는 채용 도구.

이 파일은 대화가 끊기거나 다른 사람(또는 다른 세션)이 이어받아도, 지금까지 뭘 했고 다음이 뭔지
파일만 보고 파악할 수 있도록 만든 진행 상황 기록이다. 코드가 곧 진실이므로, 아래 내용과 실제
코드가 다르면 코드를 따른다.

## 배포 상태

- 프로덕션: https://interview-sync-nu.vercel.app (GitHub `master` 브랜치 push 시 자동 배포)
- DB: Supabase (서비스 롤 키로만 접근, 브라우저 직접 접근 없음)
- 이메일: Gmail SMTP (개인 계정 — 실무 도입 시 AWS SES 등으로 교체 필요, 아래 "알려진 한계" 참고)

## 완료된 핵심 기능

- 후보자 희망시간 ↔ 면접관 캘린더 ↔ 회의실 자동 대조 및 확정
- 전원 동시 가능 시간이 없으면 충돌 최소 시간을 추천(동점 시 전부 제안)
- 후보자가 1~3순위 제출 → 면접관 전원에게 각 순위 참석 가능 여부 확인 요청 → 전원 가능한
  가장 높은 순위로 자동 확정(`lib/requestPriorityConfirmation.ts`, `lib/confirmFromPriorities.ts`)
- 확정 후 후보자가 일정 변경을 요청하면, 후보자에게 먼저 넓게(이번 주+다음 주) 가능한 시간을
  체크하게 하고 그 시간 전체를 면접관 전원에게 재확인시켜 자동 재확정(항상 재확인을 거치며,
  라이브 데이터만 보고 조용히 확정하지 않음 — `app/api/respond/[token]/route.ts`의
  `reschedule_request` 분기)
- 후보자가 처음 추천받은 시간을 전부 거절하면, 면접관 재문의 대신 후보자에게 다음 주 시간을
  체크하게 하고, 그래도 안 되면 자유 입력(가능 시점/사유)으로 리크루터에게 에스컬레이션
  (`candidate_wide_availability` kind, `app/interviews/page.tsx`의 대시보드 note 표시)
- 면접관 응답 진행률(N/M)을 재문의 라운드와 무관하게 정확히 계산 (`lib/interviewerProgress.ts`)
- 면접관·후보자·최종 확인 응답의 전체 히스토리(언제 무엇을 답했는지)를 스냅샷으로 저장해
  상세 페이지에서 열람 가능 (`answered_slots`/`answered_busy_slots`/`answered_preferred_slots`)
- 확정된 시간 자체는 매칭 로직상 항상 전원 가능이 보장되므로, 라이브 busy_slots로 재검증하지
  않고 매트릭스에 그대로 표시(자기참조 오류 수정)
- 미응답 자동 독촉 메일 + 면접 전날 리마인더 (Vercel Cron, `vercel.json`)
- 30분 단위 슬롯 그리드(when2meet 방식) UI, 히트맵 기반 수동 확정
- **리스크 관리 3종 세트** (`lib/email.ts`, `lib/confirmFromPriorities.ts`):
  - 킬 스위치 — `EMAIL_SENDING_ENABLED=false`면 코드 배포 없이 전체 자동 발송을 즉시 중단
  - 확정 메일 승인 게이트 통일 — 우선순위 자동 확정(`confirmFromPriorities`)도 매칭만 자동으로
    하고, 실제 확정 메일은 다른 경로와 동일하게 리크루터가 "확정 메일 발송" 버튼을 눌러야 나감
    (가장 위험한 메일에는 항상 사람 확인이 들어가도록 통일)
  - 발송 실패 가시화 — `sendEmail` 실패 시 `[email-failed]` 태그로 Vercel Logs에 남고, 이메일을
    보내는 **모든** 함수(확정 메일·후보자 초대·면접관 초대·재문의·최종 확인 요청)에서 실패하면
    `interview.note`에도 남아 대시보드에 바로 보임(escalated가 아니어도 note가 있으면 표시하되,
    "⚠️"가 붙거나 escalated일 때만 빨간색으로 강조).
  - 재발송 경로 — 외부로 나가는 메일마다 실패 시 실제로 재시도할 방법을 마련했다:
    - 면접관 초대(1차)·재문의: 상세 페이지 패널 배지 옆 **"재발송"** 버튼
      (`app/api/interviews/[id]/reinvite-interviewer`)
    - 최종 참석 확인(priority_confirm): 응답 요약 매트릭스 헤더의 **"재발송"** 버튼
      (`app/api/interviews/[id]/reinvite-priority-confirm`)
    - 후보자 초대·확정 메일: 실패해도 stage/confirmation_sent_at을 진행시키지 않아서, 기존
      "후보자에게 이메일 발송"·"확정 메일 발송" 버튼이 그대로 재시도 버튼으로 작동함(별도
      버튼 불필요)
    - 면접관 관리 페이지의 기존 "문의 메일 보내기"는 특정 케이스와 연결되지 않은 별도
      링크라 재시도 용도로는 안 맞아서, 위 전용 재발송 라우트들을 새로 만들었다
  - **재발송이 만드는 회귀 버그를 발견해 수정**: 발송 실패로 방치된 예전 pending 요청이
    남은 채로 새 토큰에 답하면, 예전 "해당 라운드 모든 행이 submitted인지" 로직이 그
    방치된 요청 때문에 영원히 "미완료"로 판단해 자동 확정이 절대 안 되는 문제가 있었다.
    `computeInterviewerProgress`를 "면접관별 최신 요청 기준"으로 고쳐서(하위 호환 없이
    로직 자체를 교체) 해결했고, `scripts/verify-priority-confirm-resend.js`로 회귀
    테스트를 남겼다.
  - **발송 성공 여부(`email_sent_at`)를 응답 여부와 분리해 기록** — "재발송" 버튼이
    "아직 응답을 안 함"과 "메일이 애초에 안 갔음"을 구분하지 못하고 항상 같은 모습으로
    떴었다(사용자 피드백: 발송 오류가 실제로 있었는지 대시보드에서 확인할 수 있어야
    하고, 재발송은 발송 실패가 확인된 경우에 대한 것이어야 한다는 지적). 메일을 보내는
    모든 함수/라우트가 성공 시 `response_requests.email_sent_at`을 기록하도록 하고
    (`supabase/migration_email_sent_at.sql`), 면접 상세 페이지·**목록(대시보드) 페이지·
    Week/Day 캘린더 드로어**(`app/interviews/page.tsx`, `_components/EventDrawer.tsx`)
    모두에서 면접관별로 "○ 발송됨, 응답 대기"(회색)와 "⚠️ 발송 실패"(빨간색, 상세
    페이지에서는 재발송 버튼도 강조)를 구분해서 보여준다 — 사용자가 상세보기까지
    들어가지 않고도 목록에서 바로 발송 실패를 알 수 있어야 한다고 지적해서 목록/드로어
    까지 확장했다. 단, 애초에 초대 자체를 아직 시도 안 한 상태(`stage: "created"`, 또는
    최종 확인 요청 레코드가 아직 없음)는 실패로 오인하지 않도록 걸러낸다. 이 컬럼이
    생기기 전에 이미 있던 요청들은 `email_sent_at`이 전부 null이라 그대로 두면 전부
    "발송 실패"로 오탐되므로, 마이그레이션에 `created_at`으로 채우는 1회성 백필도
    포함했다(이후 새로 생기는 요청만 코드가 실제 발송 결과로 채움).
  - **발송 실패 사유까지 노출** — "발송 실패했다"만 알아서는 채용담당자가 할 수 있는
    게 없다(사용자 피드백: 원인도 알아야 하는 거 아니냐는 지적). 메일을 보내는 모든
    함수/라우트가 캐치한 에러의 실제 메시지(`lib/email.ts`의 `emailErrorReason`, 200자
    제한)를 note/에러 응답에 그대로 포함하도록 했다 — 예: "면접관 초대 메일 발송 실패:
    OOO(사유: No recipients defined)". 존재하지 않는 이메일 주소로 실제 발송을 실패시켜
    (DB 직접 조작이 아니라 진짜 SMTP 실패) note에 사유가 정상적으로 남는 것까지
    확인했다.
  - **리마인더/독촉 메일(크론)에는 이 가시화가 빠져있던 것을 발견해 수정** — 위
    발송 실패 가시화는 최초 초대·최종 확인·확정 메일에만 적용돼 있었고, 매일 도는
    크론(`lib/sendReminders.ts`)의 리마인더는 개별 실패를 `try/catch`로 잡긴 해도
    `interview.note`나 `email_sent_at`에 전혀 기록하지 않아서, 실패해도 크론의 HTTP
    응답(아무도 안 보는)에만 남고 대시보드에는 아무 표시가 없었다(사용자 지적:
    "이미 만든 발송 오류 표시로 크론 실패도 커버되는 거 아니냐"는 질문에 코드를
    직접 확인해서 답한 것). 독촉 메일은 실패해도 `reminded_at`을 안 올려서 다음
    크론 때 자동 재시도되고, 전날 리마인더는 "내일" 하루만 대상이라 재시도 기회가
    없으므로 note에 "자동 재시도 없음"을 명시해 즉시 직접 연락하도록 안내한다.
    로컬에서 실제로 존재하지 않는 이메일 주소로 크론을 직접 실행시켜 note에 사유가
    남는 것까지 확인했다(`lib/sendReminders.test.ts`).
- **외부 헬스체크용 엔드포인트** (`app/api/health/route.ts`) — 지금까지의 발송 실패
  가시화는 전부 "사람이 버튼을 누른 순간"에만 보이는데, 새벽에 도는 Cron이나 앱 전체가
  죽는 건 그 순간 지켜보는 사람이 없어서 아무도 모른다는 문제가 있었다. 이 엔드포인트는
  단순히 페이지가 열리는지가 아니라 실제로 Supabase DB에 접근 가능한지까지 확인해서,
  UptimeRobot 같은 무료 외부 모니터링 서비스가 주기적으로 호출해 장애를 감지하고
  알림을 보낼 수 있게 한다(외부 서비스 가입·설정은 사용자가 직접 해야 함 — 아래
  "비상시 대응 방법" 참고).
- **이메일 발송 함수 테스트에 본문 내용 검증 추가** — 기존 테스트는 발송 성공/실패
  여부(stage·note·email_sent_at)만 검증해서, "SMTP는 성공했는데 코드 버그로 후보자
  이름이나 링크, 날짜가 잘못 들어간 경우"는 전혀 못 잡았다. 각 발송 함수 테스트에
  실제 `sendEmail` 호출 인자(수신자·제목·본문)를 검사하는 assertion을 추가해서,
  내용 자체가 틀리면 테스트가 실패하도록 했다.
- **슬롯 생성이 실제 배포 서버(UTC)에서 9시간 어긋나던 버그 수정** (`lib/slots.ts`) —
  업무시간(09~18시) 슬롯을 만들 때 `Date`의 로컬 시간 메서드(`setHours`/`getHours`)를
  썼는데, 이건 "그 코드가 실행되는 컴퓨터의 시간대"를 그대로 따른다. 로컬 개발
  환경은 한국 시간이라 전혀 문제가 안 보였지만, Vercel의 실제 서버는 UTC로 돌아서
  배포된 서비스에서는 "오전 9시"라고 표시되는 슬롯이 실제로는 한국 시간 오후 6시를
  가리키고 있었다. 프로덕션에 직접 `/api/slots`를 호출해서 실제로 이 어긋남을
  확인한 뒤, `lib/sendReminders.ts`에 이미 있던 것과 같은 방식(고정 9시간 오프셋을
  명시적으로 더하고 UTC 필드를 읽는 것)으로 서버 시간대와 무관하게 항상 한국 시간
  기준으로 계산하도록 고쳤다. 기존 테스트도 타임존 표시가 없는 날짜 문자열(실행
  환경에 따라 다르게 해석됨)을 쓰고 있어서 이 버그를 가려주고 있었던 것까지 확인하고,
  타임존을 명시한 문자열로 다시 작성했다.
  (2026-08-23 갱신: 당시 "클라이언트 컴포넌트는 사용자 브라우저에서 도니 문제없다"고
  판단해 `dDayLabel`, `groupBusySlotsByDay` 등을 그대로 뒀는데, 그 판단이 좁았다.
  해외에 있는 후보자의 브라우저는 로컬 시간이 한국 시간이 아니라서 같은 슬롯이 다른
  시각으로 보인다. 아래 "2026-08-23" 절 참고 — 전부 한국 시간으로 고정했다.)

### 2026-08-23 — 소요시간, 자동 검증, 한국 시간 고정, 응답 히트맵

`a9f7e8a` 이후 커밋 12개. 각 커밋 메시지에 "왜 그렇게 고쳤는지"를 적어뒀으니, 세부가
필요하면 `git log`를 보는 것이 이 문서보다 정확하다.

**① 면접 소요시간을 1급 개념으로 도입** (`f7a734c`, `c909656`)

면접을 30분 격자 위의 "한 점"으로만 다루고 있었다. 10:00에 확정된 1시간 면접의 10:30이
여전히 비어 있는 것으로 취급돼, 같은 면접관·회의실에 겹치는 면접이 또 잡힐 수 있었다.
매칭도 정합성 검사도 슬롯 문자열이 다르니 겹침으로 보지 않았다.

`lib/slots.ts`의 `INTERVIEW_DURATION_MINUTES`(대면 60분, 온라인·전화 30분) 한 곳에
소요시간을 정의하고, 점유(`occupiedSlots`)·업무시간 초과(`fitsInBusinessHours`)·구간
겹침(`interviewsOverlap`)·종료 시각 표기(`formatSlotRangeLabel`)를 모두 그 값 기준으로
계산한다. 확정·재조율·수동 확정 모든 경로가 구간 전체를 점유·해제하도록 고쳤다.

**② 테스트를 빌드와 CI에서 자동 실행** (`777b984`, `bc906ff`)

`npm test`를 사람이 직접 입력할 때만 테스트가 돌았다 — 고치고 실행을 잊으면 검증이
아예 일어나지 않는 상태였다. 두 겹으로 막았다.

- `package.json`의 `prebuild`: 빌드 직전 테스트를 돌려, 실패하면 `next build`까지 가지 못한다
- `.github/workflows/ci.yml`: push·PR마다 lint → 타입검사 → 테스트

GitHub Actions 실패가 Vercel 배포를 자동으로 막지는 않으므로, 실제 차단은 `prebuild`가
담당한다. 붙인 첫 CI 실행이 곧바로 실제 버그를 잡았다(아래 ③).

**③ 데이터 정합성 검사 + 겹침 기준 이중 배정 판정** (`c62cbf4`)

매일 1회 크론(`/api/cron/consistency-check`)으로 전체를 스캔하고, 확정 메일 발송 직전에
그 건 하나만 다시 확인한다. 위반이면 발송을 보류하고 `note`에 이유를 남긴다. 이중 배정
판정을 슬롯 문자열 일치에서 **시간 구간 겹침**으로 교체하고, 업무시간을 넘겨 끝나는 확정
건(`outside_business_hours`)을 새 위반 유형으로 추가했다(위반 6종 → 7종).

**④ 정합성 보류 시 강제 발송 버튼** (`b853549`)

`force` 옵션이 API에만 연결돼 있고 화면에서 호출하는 곳이 없어, 보류되면 담당자가 사유를
보고도 진행할 방법이 없었다. `sendConfirmationEmail`이 `held`를 함께 반환하게 해서 화면이
"그냥 실패"와 "사람의 판단을 기다리는 보류"를 문구 비교 없이 구분한다. 되돌릴 수 없는
발송이라 확인을 한 번 더 묻는다. 순위 확정(`confirm-priority`)이 메일 발송 결과를 버리고
있어 보류돼도 화면에는 발송된 것으로 보였던 문제도 함께 고쳤다.

**⑤ 후보자·면접관이 보는 시간을 한국 시간으로 고정** (`b4cb81e`, `5cb39b0`)

`Date`의 로컬 getter(`getHours` 등)는 실행 환경의 시간대를 따른다. 서버(UTC)뿐 아니라
**해외에 있는 후보자의 브라우저**에서도 어긋난다 — 한국 8/25 09:00 슬롯이 뉴욕에서는
8/24 20:00로 보이고, 그걸 보고 고른 시간이 실제로는 다른 시간이 된다.

`lib/slots.ts`에 `kstShifted`로 시프트 계산을 모으고 `kstDateLabel`/`kstTimeLabel`/
`kstHour`/`kstMinute`/`kstDayKey`를 뽑아, 응답 화면·슬롯 격자·주간/일간 캘린더·D-day를
모두 한국 시간 기준으로 고쳤다. 메일에는 `KST_NOTICE`("표시된 시간은 모두 한국 시간(KST,
UTC+9) 기준입니다.")를 넣었다 — 라벨 자체는 원래도 한국 시간이었지만 그 사실이 적혀
있지 않아 해외 수신자는 자기 지역 시간으로 읽을 수밖에 없었다.

Asia/Seoul · UTC · America/New_York · Pacific/Auckland 네 시간대에서 전부 통과를 확인한다.

**⑥ 응답 현황 히트맵** (`68a7e52`, `15a5fa8`, `6c60704`, `a58550c`)

나열식 히스토리만으로는 "지금 누가 언제 가능한지"를 한눈에 볼 수 없었다. 상세 화면에
30분 격자 히트맵을 넣었다.

- `lib/responseMatrix.ts`(순수 함수 + 테스트 22개)가 슬롯별로 `available`(응답 완료 +
  가능) / `unavailable`(응답 완료 + 불가능) / `unknown`(미응답 — 추정) / `ambiguous`를
  분류한다. **미응답을 available과 분리하는 것이 핵심이다** — `busy_slots`만 보면
  "가능하다고 답한 사람"과 "아직 답하지 않은 사람"이 똑같이 보인다.
- `ambiguous`는 확정 구간 전용이다. 확정되면 그 시간이 `busy_slots`에 추가되므로
  (`applyMatch`), 그 항목이 "본인이 안 된다고 한 것"인지 "이 면접이 잡힌 것"인지 구분할
  수 없다. `unavailable`로 세면 확정된 시간이 전원 불가능으로 빨갛게 보인다.
- `startable`은 `findMatch`와 같은 기준이라 **미응답자를 가능으로 취급한다**. 그래서
  `startable === true && unknown.length > 0`이면 통과의 근거가 답변이 아니라 침묵이라는
  뜻이고, 화면이 그 사실을 문장으로 알린다.
- 시간 축은 `/api/slots`(앞으로 5영업일)만으로 잡지 않는다. 응답에 등장한 슬롯을 모두
  합집합으로 모아야 지난 라운드가 잘리지 않는다.
- `SlotGrid`에 `estimated`(사선 무늬) / `mark`(순위 메달·확정 ●) / `readOnly` /
  `size="roomy"`를 추가했다. 색은 "불가능하다고 답한 인원 수"만, 추정 여부는 무늬라는
  **별도 채널**이다 — 하나로 합치면 추정이 확정처럼 보인다.
- 15초마다 자동 갱신(폴링). 다른 탭을 보고 있는 동안은 조회하지 않고, 펼쳐 본 슬롯은
  키로 들고 있어 갱신돼도 열린 상태가 유지된다.

Supabase Realtime은 쓰지 않았다. 테이블별 Realtime 활성화와 RLS를 맞춰야 하고, 구독 확인
직후의 변경이 유실되는 경쟁 조건과 라우트 이동 시 옛 채널이 남는 문제가 알려져 있다. 이
화면은 담당자만 보고 응답은 분 단위로 들어오므로, 실패해도 다음 주기에 저절로 복구되는
폴링이 더 적합하다. 갱신 경로를 `load()` 하나로만 뒀으니 갈아끼울 때 그 훅만 바꾸면 된다.

### 2026-08-23 (이어서) — 옛 확정 건 백필, 확정 구간의 모호함 해소

**⑦ 소요시간 도입 이전 확정 건의 `busy_slots` 백필** (`lib/backfillBusySlots.ts`,
`scripts/backfill-busy-slots.ts`)

①에서 소요시간을 도입하기 전에 확정된 면접들은 `busy_slots`에 **시작 슬롯 하나만**
들어 있다. 새로 확정되는 건은 `applyMatch`가 구간 전체를 점유하므로 정확하지만, 옛
건들은 뒷 30분이 여전히 비어 보여 같은 면접관·회의실에 겹치는 면접이 또 잡힐 수 있다.

계산(`planBusySlotsBackfill`)과 쓰기(스크립트)를 분리했다 — 계산은 순수 함수라 테스트
11개로 커버되고, 스크립트는 그 결과를 출력하거나 반영하기만 한다.

- **더하기만 한다.** 확정이 풀린 뒤에도 남아 있는 옛 슬롯을 지우지는 않는다. `busy_slots`에는
  면접관이 스스로 표시한 개인 일정도 섞여 있어서, 어떤 항목이 면접 때문에 생긴 것인지
  데이터만으로는 구분할 수 없다. 잘못 지우면 없는 시간에 면접이 잡히므로 안전한 방향만
  자동화했다.
- **미리보기가 기본이고 `--apply`를 붙여야 쓴다.** 여러 번 실행해도 결과가 같고(멱등),
  중간에 실패해도 남은 것만 다시 채워진다.
- 시작 슬롯 자체가 빠진 경우(확정 중간에 update가 실패한 반쪽 상태)도 같이 잡힌다.

실제 DB에서 돌려보니 데모 케이스 1건(이하은 — 8/25 10:30~11:30)이 정확히 이 상태였다.
면접관 2명(배지훈·오세훈)과 회의실 C가 11:00 슬롯을 점유하지 않고 있었다. `--apply`로
반영한 뒤 다시 돌려서 "채울 슬롯이 없다"가 나오는 것까지 확인했다.

```bash
npm run backfill:busy-slots            # 미리보기 — DB를 건드리지 않는다
npm run backfill:busy-slots -- --apply # 실제 반영
```

스크립트를 TypeScript로 쓰기 위해 `tsx`를 devDependency로 추가했다. `scripts/`의 기존
검증 스크립트들처럼 CommonJS로 쓰면 `lib/slots.ts`의 `occupiedSlots`·소요시간 표를
그대로 못 쓰고 복사해야 하는데, **백필의 정답이 그 표에 달려 있어서** 복사본이 원본과
어긋나는 순간 조용히 틀린 값을 쓰게 된다.

**⑧ 확정 구간의 `ambiguous`를 참석 확인 답변으로 해소** (`lib/responseMatrix.ts`)

⑥에서 확정 구간은 `ambiguous`(구분할 수 없음)로 끝냈다. 확정되면 그 시간이 `busy_slots`에
추가되므로(`applyMatch`), 그 항목이 "본인이 안 된다고 한 것"인지 "이 면접이 잡힌 것"인지
데이터만으로는 알 수 없기 때문이다.

그런데 최종 참석 확인(`priority_confirm`)의 `answered_slots`에 면접관이 그 시간에 실제로
"참석 가능"이라고 **직접 답한 기록**이 있다. 답변은 `busy_slots`보다 정확한 근거이므로,
그 기록이 있으면 `ambiguous` 대신 `available`로 분류한다.

답변은 옵션의 **시작 시간** 하나지만 그 답변이 가리키는 건 면접 구간 전체다 — 10:30
옵션에 "가능"이라고 답한 것은 10:30~11:30에 대한 답이다. 그래서 시작 슬롯을 소요시간만큼
펼쳐서 대조한다. 이게 없으면 1시간 면접의 뒷 30분 칸만 계속 모호한 채로 남고, 실제로
데모 데이터에서 한 면접관은 "11:00 시작 옵션은 불가능"이라고 답해 11:00이 `busy_slots`에
들어가 있어서, 펼치지 않으면 본인이 참석하기로 한 면접의 뒷 30분이 "불가능"으로 보인다.

확정 구간 **밖**에서는 이 답변이 `busy_slots`를 덮어쓰지 않는다. 답변 이후에 본인 사정이
생겨 다시 불가능해졌을 수 있고, 그 경우엔 `busy_slots`가 더 최신이다.

로컬에서 실제 데모 케이스로 확인했다 — 확정 시간(8/25 10:30~11:30)을 누르면 이전에는
"가능(답변 확인) —" 과 "구분할 수 없습니다" 안내가 나오던 자리에, 이제 "가능(답변 확인)
배지훈, 오세훈"이 나온다.

## 개발 중 실제로 걸린 함정 (같은 데서 또 막히지 않도록)

- **`npm run build` 뒤에 `next dev`를 띄우면 API가 404** — 프로덕션 `.next`를 dev 서버가
  물어서 라우트를 못 찾는다. 화면은 "불러오는 중..."에서 멈추고 데이터는 정상인데 API만
  404가 난다. `rm -rf .next` 후 재시작하면 해결된다.
- **Tailwind `border-primary`가 안 먹는다** — `app/globals.css`의 `* { @apply border-border }`에
  밀려 `borderColor`가 `--border` 값으로 계산된다. 테두리 색으로 상태를 표시하려면
  `outline`을 쓰는 것이 안전하다(레이아웃에도 영향이 없다).
- **JSX 줄바꿈이 공백을 먹는다** — `기준 ·` 다음 줄에 `{값}`을 두면 `기준 ·15초`로 붙는다.
  `{" "}`를 명시해야 한다.
- **vitest 설정 파일은 `.mts`여야 한다** — `vitest.config.ts`로 두면 CommonJS로 로드돼
  매 실행마다 경고가 난다.
- **`@/*` 별칭은 vitest에서 따로 설정해야 한다** — 설정이 없으면 `app/` 아래 테스트가
  개별 실패가 아니라 **"0 test"로 조용히 건너뛰어진다**. 통과 개수만 보면 검증된 것처럼
  보이므로, `Tests` 줄만 보지 말고 `Test Files` 줄도 함께 확인해야 한다.

## 알려진 한계 (실무 도입 전 반드시 해결해야 함)

- **인증이 전혀 없음** — 대시보드 URL을 아는 사람은 누구나 모든 후보자 개인정보를 보고 케이스를
  삭제할 수 있다. 포트폴리오 데모 용도로는 문제없지만(가짜 데이터만 사용), 실제 채용 데이터를
  다루려면 Supabase Auth 등으로 로그인·권한 분리부터 붙여야 한다.
- **이메일이 개인 Gmail SMTP** — 발송량 제한(일 500건), 발신 신뢰도, 계정 정지 시 전체 장애
  위험이 있다. 실무 도입 시 AWS SES 등 정식 트랜잭셔널 이메일 서비스로 교체 필요.
- **주소는 맞는 형식인데 실제로 존재하지 않는 수신자는 발송 실패로 못 잡음** — `email_sent_at`은
  `sendMail()`이 그 자리에서 에러를 던진 경우만 감지한다(형식 오류·SMTP 인증 실패·연결
  실패 등). 하지만 도메인/계정 자체가 존재하지 않는 주소로 보내면 Gmail SMTP는 일단
  "발송 접수"로 처리하고, 실제 반송(bounce)은 몇 분~몇 시간 뒤 비동기로 발생하며 그
  반송 메일은 앱이 아니라 **Gmail 계정 받은편지함**으로 간다. 즉 이 경우 화면에는
  "✔ 발송됨"으로 잘못 표시된다 — SMTP 프로토콜 자체의 구조적 한계라 코드로 해결 불가.
  실무 도입 시 AWS SES 같은 서비스의 bounce/complaint 웹훅을 붙여야 완전히 해결된다.
- **확정이 트랜잭션으로 묶여 있지 않음** — 확정 한 건이 개별 `update` 세 번으로 나뉜다
  (면접 행 → 면접관별 `busy_slots` → 회의실). 중간에 하나가 실패하면 "확정됐다고 적혀
  있는데 캘린더는 비어 있는" 반쪽 상태가 남는다. 같은 이유로 두 건이 동시에 같은 시간을
  확정하면 둘 다 검사를 통과할 수 있다. **막지 않고, 생기면 정합성 검사가 잡는 쪽을
  선택했다** — Supabase 클라이언트로 여러 테이블을 한 트랜잭션에 묶으려면 DB 함수(rpc)로
  옮겨야 해서 스코프에서 미룸.
- **미응답과 "전부 가능"이 데이터상 구분되지 않음** — `busy_slots`는 불가능한 시간의
  목록이고 기본값이 빈 배열이다. 그래서 답을 안 한 면접관과 "전부 가능"이라고 답한
  면접관이 같은 모양이 된다. 자동 흐름은 전원 응답 전에 다음 단계로 넘어가지 않아 막히지만
  (`computeInterviewerProgress`의 분모가 패널 인원 수이고, 요청 행이 없는 사람은 미응답으로
  센다), 담당자가 히트맵에서 **직접 확정하는 경로에는 그 게이트가 없다**. 히트맵이 미응답을
  사선 무늬로 구분해 보여주는 것이 현재의 대응이다.
- **`INTERVIEW_DURATION_MINUTES` 값은 가정이다** — 대면 60분, 온라인·전화 30분으로 넣었다.
  실제와 다르면 그 한 곳만 고치면 전체가 따라오지만, **건별로 소요시간을 넣는 경로가 없다.**
  90분 면접이면 시스템은 60분만 잡고 조용히 확정하고, 뒤 30분에 다른 면접이 들어와도
  겹침으로 보지 않는다.
- **회의실에 정원·위치·장비 정보가 없음** — `rooms` 테이블은 `id`, `name`, `busy_slots`뿐이다.
  매칭은 "처음 발견한 빈 방"을 쓴다(`rooms.find`). 면접관 4명 면접에 2인실이 배정돼도
  시스템은 알 방법이 없다.
- 노쇼 처리·후속조치, 평가표 제출 추적·리마인드 미구현 (스코프에서 의도적으로 제외)
- 실제 구글/아웃룩 캘린더 연동 없음 — 면접관이 수동으로 그리드에 체크하는 방식. 이 프로젝트의
  핵심 가치(반복 업무 제거)를 가장 크게 끌어올릴 수 있는 확장이지만, OAuth 연동 등 별도로
  하루 이상 걸리는 작업이라 의도적으로 미룸.
- **크론(리마인더) 자체가 통째로 실행 안 되는 것은 못 잡음** — `app/api/cron/reminders/route.ts`는
  개별 수신자 발송 실패는 이제 대시보드에 뜨지만(위 "완료된 핵심 기능" 참고), Vercel이 크론
  호출 자체를 안 하거나 함수 최상단에서 처리되지 않은 예외로 죽는 경우는 아무 데도 안 남는다.
  이걸 확실히 잡으려면 "하트비트(cron job) 모니터링"이 필요한데, 확인해보니 UptimeRobot은 이
  기능이 무료가 아니라 유료(Pro) 플랜 전용이라 지금 스코프에서는 붙이지 않기로 했다. `/api/health`
  외부 모니터링은 앱/DB 전체 장애는 잡아주지만 크론 자체의 실행 여부까지는 보장 못 한다.

## 비상시 대응 방법 (앱이 갑자기 작동 안 될 때)

Vercel(앱)과 Supabase(데이터)는 완전히 별개의 서비스라, 앱이 죽어도 데이터 자체는
그대로 살아있다. 문제 유형별로 봐야 하는 곳이 다르다:

- **사이트 자체가 안 열림** → Vercel 대시보드 → Deployments 탭에서 최근 배포가
  Error인지 확인. 문제라면 **Instant Rollback**으로 직전 정상 버전으로 즉시 복구
  (데이터는 그대로 두고 코드만 되돌리는 것이라 가장 빠르고 안전함). 단, 그 배포에
  DB 스키마 변경(마이그레이션)이 같이 있었다면 롤백해도 스키마 불일치가 남을 수
  있으니 확인 필요.
- **후보자/면접관이 표시한 시간을 지금 당장 확인해야 함** → Supabase 대시보드
  (supabase.com/dashboard) → Table Editor → `interviews`/`interviewers` 테이블에서
  직접 조회 가능(앱 상태와 무관하게 항상 열려있음). 시간은 UTC로 저장되므로
  **+9시간 하면 한국 시간**. 원본 컬럼(`busy_slots`, `preferred_slots` 등)이 읽기
  어려우면 그대로 복사해서 Claude에게 붙여넣으면 바로 가독성 있게 정리해준다.
- **채용담당자(개발자 본인이 아닌 사람)가 위 상황에서 직접 확인해야 하는 경우** →
  현재는 Supabase 로그인 권한이 없어 불가능. 계정 비밀번호를 공유하지 말고,
  Supabase의 팀원 초대 기능으로 **읽기 전용 권한**을 별도로 부여할 것(아직 미설정 —
  1인 운영 중이라 필요 시 설정).
- **앱 전체가 죽었는데 아무도 모르는 상황을 막으려면** → `/api/health`
  (`https://interview-sync-nu.vercel.app/api/health`)를 [UptimeRobot](https://uptimerobot.com)
  같은 무료 서비스에 등록해서 몇 분 간격으로 핑하게 하고, 실패 시 이메일/문자로
  알림을 받도록 설정할 것 — 계정 가입과 모니터 등록은 개발자 본인이 직접 해야 하는
  절차라 아직 미설정 상태(엔드포인트 자체는 준비됨).

## 다음 단계 (우선순위 순)

**먼저 할 것 — 2026-08-23 작업에서 남은 마무리**

1. ~~**옛 확정 건의 `busy_slots` 백필**~~ — 완료. 위 ⑦ 참고. 실제 DB에 반영했고
   (면접관 배지훈·오세훈, 회의실 면접실 C에 각각 11:00 슬롯 1개), 반영 뒤 다시
   돌려 "채울 슬롯이 없다"까지 확인했다. 스크립트는 멱등이라 앞으로도 점검용으로
   `--apply` 없이 그냥 돌려보면 된다.
2. **`INTERVIEW_DURATION_MINUTES` 실제값 확인** — 위 "알려진 한계" 참고. 값이 틀리면
   겹침 판정 전체가 조용히 어긋난다.
3. **리스크 관리 노트에 이번 작업 반영** — 소요시간·CI·강제 발송·한국 시간 고정·히트맵이
   전부 빠져 있다. 노트의 항목 02는 "테스트가 자동 실행되지 않는다"는 옛 전제로 쓰인
   부분을 이미 고쳤지만, 새 기능 자체는 추가되지 않았다.
   - 부록 A(`계산/로직 오류`) 제목이 실제 내용과 어긋난다. 그 표에는 회귀 방지(테스트),
     사후 감시(정합성 검사), 운영 누락(`과거 시간인데 확정 메일 미발송`)이 섞여 있다.
     특히 마지막 항목은 계산도 데이터도 정상이고 담당자가 발송을 놓친 경우다.
4. ~~**확정 구간 `ambiguous`를 더 정확하게**~~ — 완료. 위 ⑧ 참고.
5. **히트맵 사선 무늬를 실제 미응답 데이터로 확인** — 현재 데모 케이스는 면접관 2/2가
   응답을 마쳐 `unknown`이 없어서 무늬가 나올 조건 자체가 없다. 무늬 렌더링 자체는 임시
   값으로 확인했지만 실데이터 확인은 남았다.

**그다음 — 기존 백로그**

6. **UptimeRobot(또는 유사 서비스)에 `/api/health` 등록** — 위 "비상시 대응 방법"
   참고. 코드는 준비됐고, 외부 서비스 가입·모니터 등록·알림 수신처(이메일/전화번호)
   설정만 남음.
7. 확정 메일(`sendConfirmationEmail`)은 후보자·면접관 중 일부만 실패해도 전체를 다시
   보내는 방식이다(이미 성공한 사람도 중복 수신) — 수신자별로 성공 여부를 따로 기록해서
   실패한 사람에게만 재발송하도록 세분화하면 더 낫다. 지금은 위험하지 않은 사소한
   비효율이라 우선순위를 낮춰뒀다.
8. Supabase에 `interviewers_readable`, `response_requests_readable` 뷰 추가(선택 — 개발자
   본인의 디버깅 편의용, 포트폴리오 완성도와는 무관)
9. (실무 도입 시) Supabase Auth 붙이기, Gmail → AWS SES 전환

## 사람이 반드시 확인해야 하는 것 (자동 검증으로 줄지 않는 영역)

자동 검증은 "데이터가 자기 자신과 앞뒤가 맞는지"만 볼 수 있다. 아래는 전부 "데이터가
현실과 맞는지"이고, 그 현실이 시스템에 들어와 있지 않다. **규칙을 더 추가해도 줄지 않고,
그 정보를 데이터로 넣어야 줄어든다.**

| 확인 항목 | 시스템이 못 하는 이유 |
| --- | --- |
| 이 면접의 실제 소요시간 | 유형별 고정값만 있고, 이 건이 예외인지 알 데이터가 없음 |
| 회의실이 이 면접에 맞는지 | `rooms`에 정원·위치·장비 컬럼이 없음 |
| 그 시간이 실무적으로 괜찮은지 | "적절함"이 규칙으로 정의되지 않음 — 첫 슬롯이 규칙상 정답 |
| 직무명이 맞는지 | `position`이 메일 제목·본문에 그대로 들어가는데 대조할 원본이 없음 |
| 장소 자리의 대체 표기 | 회의실 조회 실패 시 면접 유형으로 대체되는 것이 의도된 동작이라 위반으로 정의 불가 |
| 공휴일·회의실 공사 | 시스템에 들어오지 않은 정보 |
| 면접관 조합이 직무에 적절한지 | 판단 기준이 규칙으로 정의되지 않음 |

## 검증 방법

```bash
npm run verify       # lint + 타입 체크 + 테스트 한 번에 (아래 세 개를 순서대로 실행)
npm run lint         # ESLint
npm run typecheck    # tsc --noEmit
npm test             # Vitest — lib/ 전체 + app/interviews/_components + components/slot-grid

# 시간 계산은 실행 환경의 시간대에 의존하지 않아야 한다. 세 시간대에서 모두 통과해야 정상.
TZ=UTC npx vitest run
TZ=America/New_York npx vitest run
TZ=Pacific/Auckland npx vitest run

# 아래는 로컬 dev 서버 + 실제 Supabase가 필요한 수동 통합 검증 (scripts/README.md 참고)
node scripts/verify-reschedule-flow.js
node scripts/verify-wide-availability-flow.js

# 헬스체크 엔드포인트가 실제 DB 연결까지 확인하는지
curl http://localhost:3000/api/health

# 옛 확정 건의 busy_slots에 빠진 구간이 있는지 (미리보기 — DB를 건드리지 않는다)
npm run backfill:busy-slots
```

핵심 매칭·확정 로직, 진행률 계산, 상태 파생 로직, 응답 분류(히트맵), 시간 계산은 단위
테스트로 커버된다.

**단위 테스트는 자동으로 실행된다** — `package.json`의 `prebuild`가 빌드 직전에 돌리므로
실패하면 배포까지 가지 못하고, `.github/workflows/ci.yml`이 push·PR마다 lint → 타입검사 →
테스트를 돌린다. (GitHub Actions 실패가 Vercel 배포를 자동으로 막지는 않으므로, 실제
차단은 `prebuild`가 담당한다.)

여러 API 라우트와 Supabase를 오가는 전체 흐름(재조율, 다음 주 가용성 체크 →
에스컬레이션)은 `scripts/`의 통합 검증 스크립트로 커버된다 — 이쪽은 실제 Supabase가
필요해서 CI에서 돌지 않고 수동 실행이지만, 저장소 안에 재현 가능한 코드로 존재한다.

**테스트 결과를 읽을 때 주의** — `Tests` 줄만 보면 안 된다. `@/*` 별칭이나 임포트가
깨지면 해당 파일이 개별 실패가 아니라 "0 test"로 조용히 건너뛰어지므로, `Test Files` 줄이
전체 파일 수와 맞는지 함께 확인해야 한다.
